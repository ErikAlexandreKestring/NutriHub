# Alvo do proxy de desenvolvimento do Vite (onde o backend Express está rodando).
# Em produção o frontend é servido pelo Azure Static Web Apps e /api é
# encaminhado para o App Service, então esta variável não se aplica.
VITE_API_PROXY_TARGET=http://localhost:3000
