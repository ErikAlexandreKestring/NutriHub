# Server
PORT=3000
NODE_ENV=development

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=nutrihub
DB_SSL=false

# JWT
JWT_SECRET=troque-esta-chave-em-producao
JWT_EXPIRES_IN_NUTRICIONISTA=8h
JWT_EXPIRES_IN_PACIENTE=24h

# Bcrypt
BCRYPT_SALT_ROUNDS=12
